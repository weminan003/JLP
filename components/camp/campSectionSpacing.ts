/**
 * All camp page sections use py-24/py-32/py-40 for equal breathing room
 * top and bottom. Use this on the section element (or the first inner
 * wrapper when the section has a scroll track that shouldn't count).
 */
export const CAMP_SECTION_PY = "py-24 md:py-32 lg:py-40";

/** Horizontal inset, applied to every section element. */
export const CAMP_SECTION_PX = "px-6 md:px-12 lg:px-20";
